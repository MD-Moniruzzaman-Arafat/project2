import PropTypes from 'prop-types';
const Cards = ({ data }) => {
    console.log(data)
    return (
        <div>

        </div>
    );
};

Cards.propTypes = {
    data: PropTypes.object
}

export default Cards;